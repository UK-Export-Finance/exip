/**
 * mapPercentage
 * Accepts a number or a string and returns a string with % on the end
 * @param {Number|String} percentage
 * @returns {String}
 */
const mapPercentage = (percentage: number | string) => `${percentage}%`;

export default mapPercentage;
