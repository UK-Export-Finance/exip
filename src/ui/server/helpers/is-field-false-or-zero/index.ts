/**
 * isFieldFalseOrZero
 * checks if field is false or zero and returns a boolean
 * @param {Any} field
 * @returns {Boolean}
 */
const isFieldFalseOrZero = (field: any) => field === false || field === 0;

export default isFieldFalseOrZero;
