const LOCATION = './server/cert/';

const CERTIFICATE = {
  LOCATION,
  NAME: `${LOCATION}localhost.crt`,
};

const KEY = {
  LOCATION,
  NAME: `${LOCATION}localhost.key`,
};

export const TLS = {
  CERTIFICATE,
  KEY,
};
