{% import '../components/task-list-row.njk' as taskListRow %}

{% macro render(params) %}

  {% set groups = params.groups %}

  {% for group in groups %}
    <h2 class="govuk-heading-m">{{ group.title }}</h2>

    <dl class="govuk-summary-list ukef-task-list">
      {% for task in group.tasks %}

        {{ taskListRow.render({
          row: task
        }) }}

      {% endfor %}
    </dl>

  {% endfor %}

{% endmacro %}
