{% from "govuk/components/header/macro.njk" import govukHeader %}

{{ govukHeader({
  homepageUrl: "https://www.gov.uk",
  serviceName: CONTENT_STRINGS.PRODUCT.DESCRIPTION,
  serviceUrl: START_ROUTE,
  attributes: {
    "data-cy": "header"
  }
}) }}
