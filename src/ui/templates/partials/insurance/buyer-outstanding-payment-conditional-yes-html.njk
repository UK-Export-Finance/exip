{% import '../../components/monetary-value-input.njk' as monetaryValueInput %}

<div class="govuk-grid-row">
  <span data-cy="{{FIELDS.TOTAL_OUTSTANDING.ID}}-heading" class="govuk-heading-m govuk-!-margin-bottom-4">{{ FIELDS.TOTAL_OUTSTANDING.HEADING }}</span>

  {{ monetaryValueInput.render({
    fieldId: FIELDS.TOTAL_OUTSTANDING.ID,
    label: FIELDS.TOTAL_OUTSTANDING.LABEL,
    currencyPrefixSymbol: CURRENCY_PREFIX_SYMBOL,
    submittedValue: submittedValues[FIELDS.TOTAL_OUTSTANDING.ID] or application.buyer.buyerTradingHistory[FIELDS.TOTAL_OUTSTANDING.ID],
    validationError: validationErrors.errorList[FIELDS.TOTAL_OUTSTANDING.ID],
    legendClass: LEGEND_CLASS
  }) }}

  {{ monetaryValueInput.render({
    fieldId: FIELDS.AMOUNT_OVERDUE.ID,
    label: FIELDS.AMOUNT_OVERDUE.LABEL,
    currencyPrefixSymbol: CURRENCY_PREFIX_SYMBOL,
    submittedValue: submittedValues[FIELDS.AMOUNT_OVERDUE.ID] or application.buyer.buyerTradingHistory[FIELDS.AMOUNT_OVERDUE.ID],
    validationError: validationErrors.errorList[FIELDS.AMOUNT_OVERDUE.ID],
    legendClass: LEGEND_CLASS
  }) }}

  <p class-"govuk-body">
    <a class="govuk-link" data-cy="provide-alternative-currency-link" href="{{ PROVIDE_ALTERNATIVE_CURRENCY_URL }}">{{ CONTENT_STRINGS.PROVIDE_ALTERNATIVE_CURRENCY }}</a>
  </p>
<div>