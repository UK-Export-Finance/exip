{% extends 'index.njk' %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from 'govuk/components/error-summary/macro.njk' import govukErrorSummary %}
{% from "govuk/components/warning-text/macro.njk" import govukWarningText %}
{% from 'govuk/components/radios/macro.njk' import govukRadios %}
{% from 'govuk/components/button/macro.njk' import govukButton %}

{% block pageTitle %}
  {{ CONTENT_STRINGS.PAGE_TITLE }}
{% endblock %}

{% block content %}

  {{ govukBackLink({
    text: CONTENT_STRINGS.LINKS.BACK,
    href: BACK_LINK,
    attributes: {
      "data-cy": "back-link"
    }
  }) }}

  {% if validationErrors.summary %}
    {{ govukErrorSummary({
      titleText: "There is a problem",
      errorList: validationErrors.summary
    }) }}
  {% endif %}

  <form method="POST" novalidate>

    <input type="hidden" name="_csrf" value="{{ csrfToken }}">

    {% if validationErrors.errorList[FIELDS.CAN_GET_PRIVATE_INSURANCE.ID] %}
      {% set validationError = validationErrors.errorList[FIELDS.CAN_GET_PRIVATE_INSURANCE.ID] %}
    {% endif %}

    {% if validationErrors.errorList[FIELDS.CAN_GET_PRIVATE_INSURANCE_YES.ID] %}
      {% set validationError = validationErrors.errorList[FIELDS.CAN_GET_PRIVATE_INSURANCE_YES.ID] %}
    {% endif %}

    {{ govukRadios({
      classes: "govuk-radios--inline",
      name: FIELDS.CAN_GET_PRIVATE_INSURANCE.ID,
      fieldset: {
        legend: {
          html: "<h1 class='govuk-heading-l' data-cy='heading'>" + CONTENT_STRINGS.HEADING + "</h1>"
        }
      },
      items: [
        {
          id: FIELDS.CAN_GET_PRIVATE_INSURANCE.OPTIONS.YES.ID,
          value: FIELDS.CAN_GET_PRIVATE_INSURANCE.OPTIONS.YES.VALUE,
          text: FIELDS.CAN_GET_PRIVATE_INSURANCE.OPTIONS.YES.TEXT,
          label: {
            attributes: {
              "data-cy": FIELDS.CAN_GET_PRIVATE_INSURANCE.ID + "-yes"
            }
          },
          attributes: {
            "data-cy": FIELDS.CAN_GET_PRIVATE_INSURANCE.ID + "-yes-input"
          },
          checked: submittedValues[FIELDS.CAN_GET_PRIVATE_INSURANCE.ID] === FIELDS.CAN_GET_PRIVATE_INSURANCE.OPTIONS.YES.VALUE
        },
        {
          id: FIELDS.CAN_GET_PRIVATE_INSURANCE.OPTIONS.NO.ID,
          value: FIELDS.CAN_GET_PRIVATE_INSURANCE.OPTIONS.NO.VALUE,
          text: FIELDS.CAN_GET_PRIVATE_INSURANCE.OPTIONS.NO.TEXT,
          label: {
            attributes: {
              "data-cy": FIELDS.CAN_GET_PRIVATE_INSURANCE.ID + "-no"
            }
          },
          attributes: {
            "data-cy": FIELDS.CAN_GET_PRIVATE_INSURANCE.ID + "-no-input"
          },
          checked: submittedValues[FIELDS.CAN_GET_PRIVATE_INSURANCE.ID] === FIELDS.CAN_GET_PRIVATE_INSURANCE.OPTIONS.NO.VALUE
        }
      ],
      errorMessage: validationError and {
        text: validationError.text,
        attributes: {
          "data-cy": FIELDS.CAN_GET_PRIVATE_INSURANCE.ID + "-error-message"
        }
      }
    }) }}

    {{ govukButton({
      text: CONTENT_STRINGS.BUTTONS.CONTINUE,
      attributes: {
        'data-cy': 'submit-button'
      }
    }) }}

  </form>

{% endblock %}


