{% extends 'index.njk' %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from 'govuk/components/error-summary/macro.njk' import govukErrorSummary %}
{% from "govuk/components/warning-text/macro.njk" import govukWarningText %}
{% from 'govuk/components/radios/macro.njk' import govukRadios %}
{% from 'govuk/components/button/macro.njk' import govukButton %}

{% block pageTitle %}
  {{ CONTENT_STRINGS.PAGE_TITLE }}
{% endblock %}

{% block content %}

  {{ govukBackLink({
    text: CONTENT_STRINGS.LINKS.BACK,
    href: BACK_LINK,
    attributes: {
      "data-cy": "back-link"
    }
  }) }}

  {% if validationErrors.summary %}
    {{ govukErrorSummary({
      titleText: "There is a problem",
      errorList: validationErrors.summary
    }) }}
  {% endif %}

  <form method="POST" novalidate>

    <input type="hidden" name="_csrf" value="{{ csrfToken }}">

    {% if validationErrors.errorList[FIELDS.TRIED_PRIVATE_COVER.ID] %}
      {% set validationError = validationErrors.errorList[FIELDS.TRIED_PRIVATE_COVER.ID] %}
    {% endif %}

    {% if validationErrors.errorList[FIELDS.TRIED_PRIVATE_COVER_YES.ID] %}
      {% set validationError = validationErrors.errorList[FIELDS.TRIED_PRIVATE_COVER_YES.ID] %}
    {% endif %}

    {{ govukRadios({
      classes: "govuk-radios--inline",
      name: FIELDS.TRIED_PRIVATE_COVER.ID,
      fieldset: {
        legend: {
          html: "<h1 class='govuk-heading-l' data-cy='heading'>" + CONTENT_STRINGS.HEADING + "</h1>"
        }
      },
      items: [
        {
          id: FIELDS.TRIED_PRIVATE_COVER.OPTIONS.YES.ID,
          value: FIELDS.TRIED_PRIVATE_COVER.OPTIONS.YES.VALUE,
          text: FIELDS.TRIED_PRIVATE_COVER.OPTIONS.YES.TEXT,
          label: {
            attributes: {
              "data-cy": FIELDS.TRIED_PRIVATE_COVER.ID + "-yes"
            }
          },
          attributes: {
            "data-cy": FIELDS.TRIED_PRIVATE_COVER.ID + "-yes-input"
          },
          checked: submittedValues[FIELDS.TRIED_PRIVATE_COVER.ID] === FIELDS.TRIED_PRIVATE_COVER.OPTIONS.YES.VALUE
        },
        {
          id: FIELDS.TRIED_PRIVATE_COVER.OPTIONS.NO.ID,
          value: FIELDS.TRIED_PRIVATE_COVER.OPTIONS.NO.VALUE,
          text: FIELDS.TRIED_PRIVATE_COVER.OPTIONS.NO.TEXT,
          label: {
            attributes: {
              "data-cy": FIELDS.TRIED_PRIVATE_COVER.ID + "-no"
            }
          },
          attributes: {
            "data-cy": FIELDS.TRIED_PRIVATE_COVER.ID + "-no-input"
          },
          checked: submittedValues[FIELDS.TRIED_PRIVATE_COVER.ID] === FIELDS.TRIED_PRIVATE_COVER.OPTIONS.NO.VALUE
        }
      ],
      errorMessage: validationError and {
        text: validationError.text,
        attributes: {
          "data-cy": FIELDS.TRIED_PRIVATE_COVER.ID + "-error-message"
        }
      }
    }) }}

    {{ govukButton({
      text: CONTENT_STRINGS.BUTTONS.CONTINUE,
      attributes: {
        'data-cy': 'submit-button'
      }
    }) }}

  </form>

{% endblock %}


