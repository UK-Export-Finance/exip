import locationAutocomplete from 'govuk-country-and-territory-autocomplete/dist/location-autocomplete.min';

const init = () => {
  window.openregisterLocationPicker = locationAutocomplete;
};

export default init();
